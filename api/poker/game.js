// payloads
function bet(action) {
  // require(action.type === "bet");
}
function check() {
  // require(action.type === "bet");
}
function raise() {
  // require(action.type === "bet");
}
function call() {
  // require(action.type === "bet");
}
function fold() {
  // require(action.type === "bet");
}

// internal
function endRound() {
  // require(action.type === "bet");
  // disseminate(lobbyId, increment, amounts)
}

function checkResult() {}
